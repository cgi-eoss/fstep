'use strict';

/**
 * @ngdoc service
 * @name fstepApp.HelpdeskService
 * @description
 * # HelpdeskService
 * Service in the fstepApp.
 */
angular.module('fstepApp')
  .service('HelpdeskService', [function () {
    // AngularJS will instantiate a singleton by calling "new" on this function
  }]);
